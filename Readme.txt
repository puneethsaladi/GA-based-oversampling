HOW TO USE THIS CODE

1. Collect the datasets(as .dat files) on which you want to compare the performance of SMOTE vs. Proposed Genetic Algorithm.
2. Add the path (where datasets are stored) in the code where os.chdir("") is called.
3. Run the code and get the results will be entered into Results.txt file.